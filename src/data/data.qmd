---
title: "Data"
date: "`r Sys.Date()`"
editor: 
    render-on-save: false
---

```{r}
#| label: setup

# load packages
suppressPackageStartupMessages(library(tidyverse))
suppressPackageStartupMessages(library(modelsummary))

# read data
source("./src/lib/io.R")
df <- read_csv("./data/raw/student_exam_scores.csv")
```

```{r}
#| label: data cleaning

df <- df |> mutate(
  attendance_percent = attendance_percent / 100,
  previous_scores = previous_scores / 100,
  exam_score = exam_score / 100
)
```

```{r}
#| label: reporting
#| tbl-cap: Data summary

datasummary_skim(
  df,
  fun_numeric = list(
    Mean = Mean,
    SD = SD
  )
)
```